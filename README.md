Generates a vim color scheme based on the colors specified in the input yml file.

To generate the vim color scheme, run:
```
./genrate marlin.yml > colors/marlin.vim
```

Based on: https://github.com/felipec/vim-felipec
